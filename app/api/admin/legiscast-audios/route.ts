import { AdminLegiscastAudioError, authorizeAdminLegiscastOriginal, confirmAdminLegiscastOriginal, deleteAdminLegiscastAudio, listAdminLegiscastAudios, previewAdminLegiscastAudio, retryAdminLegiscastAudioJob, updateAdminLegiscastAudio } from "@/lib/admin-legiscast-audios-server";

function failure(error: unknown) { const status = error instanceof AdminLegiscastAudioError ? error.status : 500; const message = error instanceof Error ? error.message : "Não foi possível concluir a operação."; return Response.json({ error: message }, { status, headers: { "Cache-Control": "no-store" } }); }
export async function GET() { try { return Response.json(await listAdminLegiscastAudios(), { headers: { "Cache-Control": "no-store" } }); } catch (error) { return failure(error); } }
export async function POST(request: Request) { try { if (!request.headers.get("content-type")?.includes("application/json")) throw new AdminLegiscastAudioError(415, "Envie os metadados do áudio em JSON."); const body = await request.json() as { operation?: string }; if (body.operation === "authorize-original") return Response.json(await authorizeAdminLegiscastOriginal(body as never)); if (body.operation === "confirm-original") return Response.json(await confirmAdminLegiscastOriginal(body as never)); if (body.operation === "retry") return Response.json(await retryAdminLegiscastAudioJob(body as never)); if (body.operation === "update") return Response.json(await updateAdminLegiscastAudio(body as never)); if (body.operation === "preview") return Response.json(await previewAdminLegiscastAudio(body as never)); if (body.operation === "delete") return Response.json(await deleteAdminLegiscastAudio(body as never)); throw new AdminLegiscastAudioError(400, "Operação de áudio inválida."); } catch (error) { console.error("[legiscast-audio-v2] api-failed", { message: error instanceof Error ? error.message : null }); return failure(error); } }
