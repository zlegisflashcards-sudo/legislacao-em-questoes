export const metadata = {
  title: "LegisFlashcards",
  description: "Materiais de legislação organizados para concursos.",
};

export default function LeisFlashcardsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
