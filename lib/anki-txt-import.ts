export type AnkiRow={line:number;deck:string[];pergunta:string;resposta:"Certo"|"Errado";justificativa:string;assunto:string;legislacao:string;ordem:string;titulo:string;total_artigos:string;slug:string;ultima_alteracao_legislativa:string};
export type ImportIssue={line:number;message:string};
function tsv(line:string){const out:string[]=[];let field="",quoted=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='"'){if(quoted&&line[i+1]==='"'){field+='"';i++;}else quoted=!quoted;}else if(c==='\t'&&!quoted){out.push(field);field="";}else field+=c;}out.push(field);return out;}
export function normalizeStructure(value:string){return value.normalize("NFKC").trim().replace(/\s+/g," ").toLocaleLowerCase("pt-BR");}
export function parseAnkiTxt(source:string){const lines=source.replace(/^\uFEFF/,"").replace(/\r\n?/g,"\n").split("\n");const headers=new Set(lines.filter(x=>x.startsWith("#")));if(!headers.has("#separator:tab"))throw new Error("O arquivo precisa declarar #separator:tab.");const rows:AnkiRow[]=[];const issues:ImportIssue[]=[];for(let i=0;i<lines.length;i++){const line=lines[i];if(!line||line.startsWith("#"))continue;const c=tsv(line);if(c.length!==11){issues.push({line:i+1,message:`Esperadas 11 colunas; encontradas ${c.length}.`});continue;}const answer=c[2].trim().toLocaleLowerCase("pt-BR");if(answer!=="certo"&&answer!=="errado"){issues.push({line:i+1,message:"Resposta deve ser Certo ou Errado."});continue;}if(!c[1].trim()||!c[9].trim()){issues.push({line:i+1,message:"Pergunta ou slug ausente."});continue;}rows.push({line:i+1,deck:c[0].split("::").map(x=>x.trim()).filter(Boolean),pergunta:c[1],resposta:answer==="certo"?"Certo":"Errado",justificativa:c[3],assunto:c[4],legislacao:c[5],ordem:c[6],titulo:c[7],total_artigos:c[8],slug:c[9].trim(),ultima_alteracao_legislativa:c[10]});}return{rows,issues,headers};}
export function validateImportSlug(rows:AnkiRow[],selected:string){const slugs=[...new Set(rows.map(x=>x.slug))];return{slugs,valid:slugs.length===1&&slugs[0]===selected,message:slugs.length!==1?`O arquivo contém mais de um slug: ${slugs.join(", ")}.`:slugs[0]!==selected?`O arquivo contém questões da legislação ${slugs[0]}, mas a legislação selecionada é ${selected}.`:null};}
export function structureIdForDeck(deck:string[],nodes:{id:number;parent_id:number|null;nome:string}[]){let parent:number|null=null;for(const segment of deck.slice(1)){const normalized=normalizeStructure(segment);const found=nodes.find(n=>n.parent_id===parent&&normalizeStructure(n.nome)===normalized);if(!found)return null;parent=found.id;}return parent;}
